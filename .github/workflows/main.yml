on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v3

      - name: "Login to Dockerhup"
        run: docker login --username mohamednasr22 --password msi01014603733
        
