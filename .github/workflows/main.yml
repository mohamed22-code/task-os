on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v3

      - name: 'Login to Dockerhup'
        run: docker login --username mohamednasr22 --password dckr_pat_U0M_6etOi3gA9Nc09r_6QOIf-gw